{
  "name": "@assessment/utils",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint \"src/**/*.ts\"",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@assessment/types": "workspace:*"
  },
  "devDependencies": {
    "@assessment/tsconfig": "workspace:*",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
