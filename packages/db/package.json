{
  "name": "@assessment/db",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "db:push": "drizzle-kit push",
    "lint": "eslint \"src/**/*.ts\"",
    "clean": "rimraf dist drizzle"
  },
  "dependencies": {
    "@assessment/types": "workspace:*",
    "drizzle-orm": "^0.39.0",
    "@neondatabase/serverless": "^1.0.0"
  },
  "devDependencies": {
    "@assessment/tsconfig": "workspace:*",
    "drizzle-kit": "^0.30.0",
    "typescript": "^5.8.0",
    "dotenv": "^16.4.0"
  }
}
